/**
 * Stub Prisma client export
 * Database functionality has been removed from this project
 * This file provides mock implementation to satisfy imports during build
 */

// Import the stub PrismaClient
import { PrismaClient } from './prisma-client-stub'

// Create a mock prisma instance
export const prisma = new PrismaClient()

// Export default as well for compatibility
export default prisma